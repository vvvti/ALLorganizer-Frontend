import React from 'react';
import {StyledFooter} from './Footer.styled';

export const Footer: React.FC = () => {
    return (
        <StyledFooter>
           Foote
        </StyledFooter>
    )
}

